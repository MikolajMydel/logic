import Popup from './Popup';

class ProjectPopup extends Popup {
    style = {
        maxWidth: '440px',
        height: '400px',
    }
    render(){
        return super.render((
            null
        ));
    }
}

export default ProjectPopup;
